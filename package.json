{
  "name": "bichosolto",
  "version": "1.0.0",
  "description": "Plataforma de resgate e adoção de animais",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "prepare-railway": "npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "animal",
    "rescue",
    "adoption",
    "ong"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
} 